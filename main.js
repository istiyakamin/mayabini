import Potaka from "./lib/parser"

let a = Potaka.parse(`
ধরি ক = ১০;           // নাম্বার
ধরি খ = ২ * ৫ + ২.৬;  // দশমিক নাম্বার
ধরি ব = সত্য;          // বুলিয়ান
ধরি এ = [১,৩,৪];      // এ্যারে

/* অবজেক্ট ডিফাইন করা */
ধরি অ = {
    নাম : "ইসতিয়াক আমিন শান্ত",
    শখ : "আকাশ কুসুম স্বপ্ন দেখা"
}

দেখাও(ক);
`);


console.log(a);